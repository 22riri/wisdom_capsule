<%= simple_form_for [@timecapsule, @memorybox] do |f| %>
  <div class="form-inputs">
    <%= f.input :title %>
  </div>

  <div class="form-actions mt-4">
    <%= f.button :submit, class: "btn btn-primary mb-4" %>
  </div>
<% end %>
